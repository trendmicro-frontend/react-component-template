import React, { Component } from 'react';

export default class extends Component {
    static propTypes = {
    };
    static defaultProps = {
    };

    state = {
    };
    actions = {
    };

    render() {
        return (
            <div>Hello</div>
        );
    }
}
