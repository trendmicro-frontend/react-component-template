import React, { PureComponent } from 'react';

class Component extends PureComponent {
    static propTypes = {
    };
    static defaultProps = {
    };

    state = {
    };
    actions = {
    };

    render() {
        return (
            <div>Hello</div>
        );
    }
}

export default Component;
